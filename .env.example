# Optional external Postgres URL for `next dev`. Netlify Database configures
# itself automatically on Netlify, so do not add this there unless overriding it.
DATABASE_URL=

# Email that receives administrator permissions for dataset imports.
ADMIN_EMAIL=

# Web client ID for Google Identity Services (not a secret).
GOOGLE_CLIENT_ID=
